---
title: Quickstart
description: Get your Sistine Starter project running in minutes.
---

## Prerequisites

- Node.js 20.9+
- pnpm 10+
- PostgreSQL database (Supabase, Neon, or Vercel Postgres recommended)

## Installation

### 1. Clone and install dependencies

```bash
git clone https://github.com/Idea-To-Business/sistine-starter-vibe-to-production.git
cd sistine-starter-vibe-to-production
pnpm install
```

### 2. Configure environment variables

Copy the example environment file and fill in your values:

```bash
cp .env.example .env
```

At minimum, you need:

```ini
DATABASE_URL="postgresql://user:password@host/db?sslmode=require"
BETTER_AUTH_SECRET="at-least-32-characters-random-key"
BETTER_AUTH_URL="http://localhost:3000"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
```

See [Environment Variables](./environment) for the full list.

### 3. Set up the database

```bash
pnpm db:push
```

This pushes the Drizzle schema to your database. For production, use migrations:

```bash
pnpm db:generate
pnpm db:migrate
```

### 4. Create an admin account

```bash
pnpm admin:setup
```

### 5. Start the dev server

```bash
pnpm dev
```

Visit `http://localhost:3000` to see your app.

## What's Next?

- [Project Structure](./project-structure) — Understand the codebase layout
- [Authentication](./auth) — Configure auth providers
- [Payments](./payments) — Set up billing and subscriptions
- [AI Features](./ai) — Configure AI integrations
